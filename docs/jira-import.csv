Summary,Issue Type,Description,Epic Name,Epic Link,Labels
PHASE 0 - Foundations,Epic,"Scaffold, route tree, code splitting.",PHASE 0 - Foundations,,phase-0
PHASE 1 - Backend v1,Epic,"Express + Prisma + Postgres API. Written for you; your job is to read, question and run it.",PHASE 1 - Backend v1,,phase-1
PHASE 2 - Auth & RBAC (frontend),Epic,"Axios instance, AuthContext, login, interceptors, guards, RBAC.",PHASE 2 - Auth & RBAC (frontend),,phase-2
PHASE 3 - API layer & data-fetching hooks,Epic,"Typed API modules, useFetch with abort, useReducer fetch state, four-state rendering.",PHASE 3 - API layer & data-fetching hooks,,phase-3
PHASE 4 - Recipes,Epic,"List, detail, the big useReducer form, photo upload, delete.",PHASE 4 - Recipes,,phase-4
PHASE 5 - Find recipes,Epic,"The perf-flavoured phase: debounce, transitions, URL state, pagination.",PHASE 5 - Find recipes,,phase-5
PHASE 6 - Meal plan,Epic,"Weekly grid, add-to-plan, macro totals, keyboard nav, localStorage prefs.",PHASE 6 - Meal plan,,phase-6
PHASE 7 - Todos & shopping list,Epic,"Prep-timer todos, countdown, aggregated shopping list, print view.",PHASE 7 - Todos & shopping list,,phase-7
PHASE 8 - Dashboard,Epic,"Today's panel, week-at-a-glance, suggestions.",PHASE 8 - Dashboard,,phase-8
PHASE 9 - Performance pass,Epic,"Measure first, then memo, context splitting, virtualization, boundaries, prefetch.",PHASE 9 - Performance pass,,phase-9
PHASE 10 - Stretch,Epic,"React 19 form APIs, TanStack Query, useSyncExternalStore, tests, deploy.",PHASE 10 - Stretch,,phase-10
MP-01 - Scaffold the frontend and lock the folder structure,Story,"*Concepts:* project structure, path aliases, TS config

h3. Subtasks
* {{npm create vite@latest client -- --template react-ts}} inside {{meal-planner/}}
* Install: {{react-router-dom axios antd dayjs}}
* Create the feature-first folder tree from cheatsheet section 5 - empty folders with a .gitkeep are fine
* Configure the {{@/}} path alias in both {{vite.config.ts}} and {{tsconfig.app.json}}
* {{git init}}, first commit, .gitignore covers node_modules, dist, .env

h3. Acceptance criteria
* {{npm run dev}} serves a page at localhost
* {{import x from '@/utils/foo'}} resolves - no red squiggle in VS Code, and {{npm run build}} passes
* Folder tree matches: api/ app/ components/ config/ constants/ context/ features/ hooks/ pages/ routes/ services/ types/ utils/
* {{npm run build}} produces zero TS errors

h3. Hints
The alias needs {{resolve.alias}} in Vite *and* {{paths}} in tsconfig; they are two separate systems and missing either one gives you a confusing half-working state.

h3. Definition of done
* All acceptance criteria personally verified in the browser
* {{npm run build}} - zero TS errors
* {{npm run lint}} - zero warnings (incl. react-hooks/exhaustive-deps; if disabled, comment why on that line)
* Loading / error / empty / success all render, if the ticket touches data
* Every effect that subscribes, unsubscribes
* Every fetch is abortable
* No index keys on dynamic lists
* A note in LEARNING.md - the concept, the Angular equivalent, and the one thing that surprised you",,PHASE 0 - Foundations,frontend setup tooling
MP-02 - Route tree with nested layouts,Story,"*Concepts:* createBrowserRouter, layout routes, <Outlet />, NavLink

*Angular parallel:* the route array is {{Routes}}, a layout route is a parent component with {{<router-outlet>}}, {{<Outlet />}} *is* {{<router-outlet>}}.

h3. Subtasks
* {{src/app/router.tsx}} exporting a createBrowserRouter tree
* {{AppLayout}} - header + sidebar nav + <Outlet />
* Placeholder pages: Dashboard /, Recipes /recipes, Recipe detail /recipes/:id, Planner /planner, Todos /todos, Shopping /shopping, Login /login, /403, * NotFound
* {{src/constants/routes.ts}} - every path as a constant, no string literals in components
* {{<RouterProvider router={router} />}} in main.tsx

h3. Acceptance criteria
* All nine routes render their placeholder
* The sidebar highlights the active route via NavLink's isActive
* /recipes/123 renders the detail page and useParams() reads ""123""
* A junk URL like /nope renders NotFound, not a blank screen
* Nav links do not cause a full page reload (check the Network tab)

h3. Definition of done
* All acceptance criteria personally verified in the browser
* {{npm run build}} - zero TS errors
* {{npm run lint}} - zero warnings (incl. react-hooks/exhaustive-deps; if disabled, comment why on that line)
* Loading / error / empty / success all render, if the ticket touches data
* Every effect that subscribes, unsubscribes
* Every fetch is abortable
* No index keys on dynamic lists
* A note in LEARNING.md - the concept, the Angular equivalent, and the one thing that surprised you",,PHASE 0 - Foundations,frontend routing
MP-03 - Route-level code splitting,Story,"*Concepts:* React.lazy, <Suspense>, bundle analysis

h3. Subtasks
* Convert every route element except the layout and Login to {{lazy(() => import(...))}}
* A real <PageSkeleton /> fallback - not a spinner, not blank
* Wrap lazy elements in <Suspense> via a small withSuspense() helper
* {{npm run build}} and read the chunk list

h3. Acceptance criteria
* dist/assets/ contains a separate JS chunk per lazy route
* Network tab (throttled to Slow 3G) shows a chunk fetched *on first navigation* to each route, not at boot
* The skeleton is visible during that fetch - no flash of blank
* Initial JS transferred is measurably smaller than before this ticket (write both numbers in the PR description)

h3. Definition of done
* All acceptance criteria personally verified in the browser
* {{npm run build}} - zero TS errors
* {{npm run lint}} - zero warnings (incl. react-hooks/exhaustive-deps; if disabled, comment why on that line)
* Loading / error / empty / success all render, if the ticket touches data
* Every effect that subscribes, unsubscribes
* Every fetch is abortable
* No index keys on dynamic lists
* A note in LEARNING.md - the concept, the Angular equivalent, and the one thing that surprised you",,PHASE 0 - Foundations,frontend routing performance
MP-10 - Server scaffold,Story,"These are mine to write. Your job is to read the code, ask about anything unclear, and run it. Do not skip the reading - the frontend tickets assume you know the contract.

h3. Subtasks
* Express 5 + TS + tsx watch mode, {{server/}} folder
* Config via .env (DATABASE_URL, JWT_SECRET, PORT, CLIENT_ORIGIN)
* CORS with credentials: true, cookie-parser, JSON body limit
* {{GET /health}} -> {{{ ok: true }}}

h3. Acceptance criteria
* {{curl localhost:4000/health}} returns 200 JSON
* Server restarts on save

h3. Definition of done
* All acceptance criteria personally verified in the browser
* {{npm run build}} - zero TS errors
* {{npm run lint}} - zero warnings (incl. react-hooks/exhaustive-deps; if disabled, comment why on that line)
* Loading / error / empty / success all render, if the ticket touches data
* Every effect that subscribes, unsubscribes
* Every fetch is abortable
* No index keys on dynamic lists
* A note in LEARNING.md - the concept, the Angular equivalent, and the one thing that surprised you",,PHASE 1 - Backend v1,backend
MP-11 - Prisma schema + Postgres,Story,"These are mine to write. Your job is to read the code, ask about anything unclear, and run it. Do not skip the reading - the frontend tickets assume you know the contract.

h3. Subtasks
* docker-compose.yml with Postgres 16 (or point at Homebrew Postgres)
* Full schema per Section 4 of the roadmap, reviewed by you before migrating
* {{prisma migrate dev --name init}}
* Seed script: ~40 ingredients, 8 recipes with prep items, 2 users (admin + regular)

h3. Acceptance criteria
* {{npx prisma studio}} shows seeded data
* Re-running the seed is idempotent

h3. Definition of done
* All acceptance criteria personally verified in the browser
* {{npm run build}} - zero TS errors
* {{npm run lint}} - zero warnings (incl. react-hooks/exhaustive-deps; if disabled, comment why on that line)
* Loading / error / empty / success all render, if the ticket touches data
* Every effect that subscribes, unsubscribes
* Every fetch is abortable
* No index keys on dynamic lists
* A note in LEARNING.md - the concept, the Angular equivalent, and the one thing that surprised you",,PHASE 1 - Backend v1,backend database
MP-12 - Auth endpoints,Story,"These are mine to write. Your job is to read the code, ask about anything unclear, and run it. Do not skip the reading - the frontend tickets assume you know the contract.

h3. Subtasks
* POST /auth/register, POST /auth/login, POST /auth/refresh, POST /auth/logout, GET /auth/me
* Access token 15min in the JSON body; refresh token 7d in an httpOnly, sameSite cookie
* requireAuth middleware; requireRole(...roles) middleware

h3. Acceptance criteria
* Login sets the cookie and returns {{{ user, accessToken }}}
* /auth/me 401s without a token
* /auth/refresh issues a new access token from the cookie alone

h3. Definition of done
* All acceptance criteria personally verified in the browser
* {{npm run build}} - zero TS errors
* {{npm run lint}} - zero warnings (incl. react-hooks/exhaustive-deps; if disabled, comment why on that line)
* Loading / error / empty / success all render, if the ticket touches data
* Every effect that subscribes, unsubscribes
* Every fetch is abortable
* No index keys on dynamic lists
* A note in LEARNING.md - the concept, the Angular equivalent, and the one thing that surprised you",,PHASE 1 - Backend v1,backend auth
MP-13 - Recipe + ingredient CRUD,Story,"These are mine to write. Your job is to read the code, ask about anything unclear, and run it. Do not skip the reading - the frontend tickets assume you know the contract.

h3. Subtasks
* GET /ingredients, GET /recipes (paginated, ?q=, ?ingredientIds=), GET /recipes/:id, POST/PUT/DELETE /recipes
* zod validation -> 422 {{{ message, errors: { field: msg } }}}
* Photo upload: POST /uploads (multer, local disk) returning a URL

h3. Acceptance criteria
* Validation failures return the 422 shape the frontend forms expect
* Delete is owner-or-admin only

h3. Definition of done
* All acceptance criteria personally verified in the browser
* {{npm run build}} - zero TS errors
* {{npm run lint}} - zero warnings (incl. react-hooks/exhaustive-deps; if disabled, comment why on that line)
* Loading / error / empty / success all render, if the ticket touches data
* Every effect that subscribes, unsubscribes
* Every fetch is abortable
* No index keys on dynamic lists
* A note in LEARNING.md - the concept, the Angular equivalent, and the one thing that surprised you",,PHASE 1 - Backend v1,backend
"MP-14 - Plan, todo and aggregation endpoints",Story,"These are mine to write. Your job is to read the code, ask about anything unclear, and run it. Do not skip the reading - the frontend tickets assume you know the contract.

h3. Subtasks
* GET/POST/DELETE /meal-plan (?from=&to=)
* GET /meal-plan/macros?date= - daily macro totals computed in SQL
* GET /shopping-list?from=&to= - aggregated, deduped by ingredient + unit
* GET /todos?from=&to=, PATCH /todos/:id (toggle done)
* Todo regeneration on plan write, preserving isDone for surviving todos

h3. Acceptance criteria
* Adding a plan entry with prep items creates todos with correct dueAt
* Deleting the entry removes its *undone* todos
* Macros endpoint matches a hand-calculated example

h3. Definition of done
* All acceptance criteria personally verified in the browser
* {{npm run build}} - zero TS errors
* {{npm run lint}} - zero warnings (incl. react-hooks/exhaustive-deps; if disabled, comment why on that line)
* Loading / error / empty / success all render, if the ticket touches data
* Every effect that subscribes, unsubscribes
* Every fetch is abortable
* No index keys on dynamic lists
* A note in LEARNING.md - the concept, the Angular equivalent, and the one thing that surprised you",,PHASE 1 - Backend v1,backend
MP-20 - Axios instance + token store,Story,"*Concepts:* service module, module-scoped state, why the access token is not in localStorage

h3. Subtasks
* {{src/api/axiosInstance.ts}} - baseURL from a typed config/env.ts, withCredentials: true, timeout
* {{src/services/tokenStore.ts}} - access token held in a module variable (memory), not localStorage
* {{src/api/endpoints.ts}} - URL constants

h3. Acceptance criteria
* No component imports axios directly - only {{@/api/axiosInstance}}
* A hard refresh loses the in-memory access token and the app recovers via /auth/refresh (proven in MP-23)
* You can explain in one sentence why the refresh token is an httpOnly cookie and the access token is not

h3. Definition of done
* All acceptance criteria personally verified in the browser
* {{npm run build}} - zero TS errors
* {{npm run lint}} - zero warnings (incl. react-hooks/exhaustive-deps; if disabled, comment why on that line)
* Loading / error / empty / success all render, if the ticket touches data
* Every effect that subscribes, unsubscribes
* Every fetch is abortable
* No index keys on dynamic lists
* A note in LEARNING.md - the concept, the Angular equivalent, and the one thing that surprised you",,PHASE 2 - Auth & RBAC (frontend),frontend auth api-layer
MP-21 - AuthContext,Story,"*Concepts:* createContext, provider, custom consumer hook, *memoized context value*

*Angular parallel:* this is your AuthService - but unlike a service, the context value's *identity* matters. A new object every render re-renders every consumer.

h3. Subtasks
* AuthContext with {{{ user, status: 'loading' | 'authed' | 'guest', login, logout }}}
* Bootstrap effect: call /auth/me on mount, with an {{alive}} flag in the cleanup
* login / logout wrapped in useCallback
* Context value wrapped in useMemo
* useAuth() hook that throws a clear error when used outside the provider
* {{src/app/providers.tsx}} composing providers so main.tsx stays flat

h3. Acceptance criteria
* status starts as 'loading' - never 'guest' before /auth/me resolves (otherwise refresh flashes the login page)
* Calling useAuth() outside the provider throws a readable error, not ""Cannot read property of null""
* Add a console.log in a consumer: it does *not* log on unrelated parent re-renders
* Unmounting mid-request produces no ""state update on unmounted component"" warning

h3. Hints
Do the useMemo deliberately: build it *without* memoization first, watch a consumer re-render on every keystroke elsewhere, then add it. That is the lesson.

h3. Definition of done
* All acceptance criteria personally verified in the browser
* {{npm run build}} - zero TS errors
* {{npm run lint}} - zero warnings (incl. react-hooks/exhaustive-deps; if disabled, comment why on that line)
* Loading / error / empty / success all render, if the ticket touches data
* Every effect that subscribes, unsubscribes
* Every fetch is abortable
* No index keys on dynamic lists
* A note in LEARNING.md - the concept, the Angular equivalent, and the one thing that surprised you",,PHASE 2 - Auth & RBAC (frontend),frontend auth hooks
MP-22 - Login page,Story,"*Concepts:* controlled inputs, submit lifecycle, finally, redirect-back

h3. Subtasks
* Controlled email + password form, client-side validation before the network call
* submitting state guarding double-submit, reset in finally
* Server field errors (422) mapped onto the inputs
* On success redirect to location.state.from if present, else /
* Logout button in the header

h3. Acceptance criteria
* Double-clicking Submit fires exactly one request (verify in the Network tab)
* A failed login re-enables the button - the finally path is exercised
* Wrong password shows the server's message, not a blank form
* Visiting /planner while logged out, then logging in, lands you back on /planner

h3. Definition of done
* All acceptance criteria personally verified in the browser
* {{npm run build}} - zero TS errors
* {{npm run lint}} - zero warnings (incl. react-hooks/exhaustive-deps; if disabled, comment why on that line)
* Loading / error / empty / success all render, if the ticket touches data
* Every effect that subscribes, unsubscribes
* Every fetch is abortable
* No index keys on dynamic lists
* A note in LEARNING.md - the concept, the Angular equivalent, and the one thing that surprised you",,PHASE 2 - Auth & RBAC (frontend),frontend auth forms
"MP-23 - Interceptors: attach token, refresh on 401",Story,"*Concepts:* cross-cutting concerns, request queueing, error normalisation

*Angular parallel:* this is your HttpInterceptor - same idea, different API.

h3. Subtasks
* Request interceptor attaching {{Authorization: Bearer}}
* Response interceptor: on 401, refresh once, retry the original request
* Queue concurrent 401s so N failed requests trigger *one* refresh
* Refresh failure -> clear tokens -> redirect to /login
* Normalise all errors to {{{ status, message, fields? }}} before rejecting

h3. Acceptance criteria
* Set the access token TTL to 10s on the server, sit on a page, and watch a request 401 -> refresh -> retry -> succeed, invisibly to the user
* Fire three requests simultaneously against an expired token: exactly one /auth/refresh in the Network tab, all three succeed
* A 401 on /auth/refresh itself does not loop infinitely
* Every catch block in the app reads err.message - no err.response?.data?.message anywhere outside the interceptor

h3. Definition of done
* All acceptance criteria personally verified in the browser
* {{npm run build}} - zero TS errors
* {{npm run lint}} - zero warnings (incl. react-hooks/exhaustive-deps; if disabled, comment why on that line)
* Loading / error / empty / success all render, if the ticket touches data
* Every effect that subscribes, unsubscribes
* Every fetch is abortable
* No index keys on dynamic lists
* A note in LEARNING.md - the concept, the Angular equivalent, and the one thing that surprised you",,PHASE 2 - Auth & RBAC (frontend),frontend auth api-layer
MP-24 - Route guards,Story,"*Concepts:* guards as layout routes, <Navigate>, redirect state

h3. Subtasks
* ProtectedRoute - loading -> full-page loader; guest -> {{<Navigate to=""/login"" state={{ from: location }} />}}; else <Outlet />
* Restructure the router so every private route is a child of it
* /403 page

h3. Acceptance criteria
* Logged out, every private URL redirects to /login; /login itself does not
* Hard-refreshing a private page does *not* flash the login screen before restoring the session
* The browser Back button after a guard redirect does not trap you in a loop (replace is set)

h3. Definition of done
* All acceptance criteria personally verified in the browser
* {{npm run build}} - zero TS errors
* {{npm run lint}} - zero warnings (incl. react-hooks/exhaustive-deps; if disabled, comment why on that line)
* Loading / error / empty / success all render, if the ticket touches data
* Every effect that subscribes, unsubscribes
* Every fetch is abortable
* No index keys on dynamic lists
* A note in LEARNING.md - the concept, the Angular equivalent, and the one thing that surprised you",,PHASE 2 - Auth & RBAC (frontend),frontend auth routing
MP-25 - RBAC,Story,"*Concepts:* derived permissions with useMemo, guard component, declarative UI gating

h3. Subtasks
* {{constants/roles.ts}} - ROLES, Permission union, ROLE_PERMISSIONS map
* usePermission() returning can / canAny / hasRole, permissions built into a Set inside useMemo
* RoleRoute layout guard -> /403
* {{<Can perm=""..."">}} wrapper component with a fallback prop
* An /admin/ingredients page behind ADMIN

h3. Acceptance criteria
* A USER navigating to /admin/ingredients lands on /403; an ADMIN sees the page
* Delete buttons on recipes you do not own are absent for USER, present for ADMIN
* With the button hidden, curling the DELETE endpoint as that user still returns *403* - client RBAC is UX, the server is the gate. Prove this and note it in the ticket.
* Permission set is not recomputed on every render (log inside the useMemo)

h3. Definition of done
* All acceptance criteria personally verified in the browser
* {{npm run build}} - zero TS errors
* {{npm run lint}} - zero warnings (incl. react-hooks/exhaustive-deps; if disabled, comment why on that line)
* Loading / error / empty / success all render, if the ticket touches data
* Every effect that subscribes, unsubscribes
* Every fetch is abortable
* No index keys on dynamic lists
* A note in LEARNING.md - the concept, the Angular equivalent, and the one thing that surprised you",,PHASE 2 - Auth & RBAC (frontend),frontend auth rbac
MP-30 - Typed API modules,Story,"*Concepts:* feature-first API layer, generics, DTO types

h3. Subtasks
* {{types/api.types.ts}} - ApiResponse<T>, ApiError, Paginated<T>
* features/recipes/api/recipesApi.ts, features/ingredients/..., features/plan/...
* Every method accepts an optional AbortSignal

h3. Acceptance criteria
* Zero {{api.get(...)}} calls inside any component or page file (grep to prove it)
* Every API function has an explicit return type - no inferred any
* Mistyping a response field is a compile error

h3. Definition of done
* All acceptance criteria personally verified in the browser
* {{npm run build}} - zero TS errors
* {{npm run lint}} - zero warnings (incl. react-hooks/exhaustive-deps; if disabled, comment why on that line)
* Loading / error / empty / success all render, if the ticket touches data
* Every effect that subscribes, unsubscribes
* Every fetch is abortable
* No index keys on dynamic lists
* A note in LEARNING.md - the concept, the Angular equivalent, and the one thing that surprised you",,PHASE 3 - API layer & data-fetching hooks,frontend api-layer typescript
MP-31 - useFetch-style hook with abort,Story,"*Concepts:* useEffect cleanup, AbortController, race conditions

h3. Subtasks
* A hook that takes a fetcher + deps and returns {{{ loading, data, error, refetch }}}
* AbortController created per effect run, aborted in cleanup
* Swallow cancellation errors; surface real ones

h3. Acceptance criteria
* Navigating away mid-request shows a cancelled request in the Network tab and logs no error
* Rapidly changing the dependency 5 times leaves the *last* response rendered - never an out-of-order earlier one (throttle the network and prove it)
* refetch() re-runs without remounting the component

h3. Hints
The stale-response race is the real lesson. Even with abort, reason about what happens if response 3 lands after response 5. An {{ignore}} boolean in the cleanup closure is the belt to abort's braces.

h3. Definition of done
* All acceptance criteria personally verified in the browser
* {{npm run build}} - zero TS errors
* {{npm run lint}} - zero warnings (incl. react-hooks/exhaustive-deps; if disabled, comment why on that line)
* Loading / error / empty / success all render, if the ticket touches data
* Every effect that subscribes, unsubscribes
* Every fetch is abortable
* No index keys on dynamic lists
* A note in LEARNING.md - the concept, the Angular equivalent, and the one thing that surprised you",,PHASE 3 - API layer & data-fetching hooks,frontend hooks data-fetching
MP-32 - useReducer for fetch state,Story,"*Concepts:* useReducer, discriminated unions, stable dispatch

h3. Subtasks
* Model fetch state as a reducer with FETCH_START / SUCCESS / ERROR
* Type Action as a discriminated union
* Refactor MP-31's hook onto it

h3. Acceptance criteria
* Impossible states are unrepresentable - you cannot have loading: true and error set at once
* dispatch is passed to a child without useCallback and you can explain why that is safe
* Adding a new action without handling it in the switch is a TS error

h3. Definition of done
* All acceptance criteria personally verified in the browser
* {{npm run build}} - zero TS errors
* {{npm run lint}} - zero warnings (incl. react-hooks/exhaustive-deps; if disabled, comment why on that line)
* Loading / error / empty / success all render, if the ticket touches data
* Every effect that subscribes, unsubscribes
* Every fetch is abortable
* No index keys on dynamic lists
* A note in LEARNING.md - the concept, the Angular equivalent, and the one thing that surprised you",,PHASE 3 - API layer & data-fetching hooks,frontend hooks typescript
MP-33 - Four-state rendering as a shared pattern,Story,"*Concepts:* conditional rendering, component composition, children as a slot

h3. Subtasks
* {{<QueryState loading error empty>}} wrapper, or an equivalent convention you apply consistently
* <PageSkeleton />, <ErrorState onRetry />, <EmptyState /> in components/ui/

h3. Acceptance criteria
* Every data-driven page renders something meaningful in all four states
* The error state has a working Retry that re-issues the request
* Empty != loading - an empty result never shows a skeleton forever

h3. Definition of done
* All acceptance criteria personally verified in the browser
* {{npm run build}} - zero TS errors
* {{npm run lint}} - zero warnings (incl. react-hooks/exhaustive-deps; if disabled, comment why on that line)
* Loading / error / empty / success all render, if the ticket touches data
* Every effect that subscribes, unsubscribes
* Every fetch is abortable
* No index keys on dynamic lists
* A note in LEARNING.md - the concept, the Angular equivalent, and the one thing that surprised you",,PHASE 3 - API layer & data-fetching hooks,frontend components ux
MP-40 - Recipe list page,Story,"*Concepts:* props, lists & keys, composition

h3. Subtasks
* RecipeCard (presentational, no data fetching) + RecipeList + RecipesPage
* Card shows name, photo or placeholder, calories/protein, ingredient count

h3. Acceptance criteria
* Page is thin - fetch + layout only, zero business logic
* Keys are recipe ids
* RecipeCard receives everything via props and could be rendered from a static array with no changes

h3. Definition of done
* All acceptance criteria personally verified in the browser
* {{npm run build}} - zero TS errors
* {{npm run lint}} - zero warnings (incl. react-hooks/exhaustive-deps; if disabled, comment why on that line)
* Loading / error / empty / success all render, if the ticket touches data
* Every effect that subscribes, unsubscribes
* Every fetch is abortable
* No index keys on dynamic lists
* A note in LEARNING.md - the concept, the Angular equivalent, and the one thing that surprised you",,PHASE 4 - Recipes,frontend components recipes
MP-41 - Recipe detail page,Story,"*Concepts:* route params, dependent fetching

h3. Subtasks
* /recipes/:id fetching by param; ingredients, macros, prep items, notes
* Handle a 404 id gracefully

h3. Acceptance criteria
* Changing the id in the URL refetches
* A bogus id shows a not-found state, not a crash

h3. Definition of done
* All acceptance criteria personally verified in the browser
* {{npm run build}} - zero TS errors
* {{npm run lint}} - zero warnings (incl. react-hooks/exhaustive-deps; if disabled, comment why on that line)
* Loading / error / empty / success all render, if the ticket touches data
* Every effect that subscribes, unsubscribes
* Every fetch is abortable
* No index keys on dynamic lists
* A note in LEARNING.md - the concept, the Angular equivalent, and the one thing that surprised you",,PHASE 4 - Recipes,frontend routing recipes
MP-42 - Recipe create/edit form,Story,"*Concepts:* useReducer for a complex form, dynamic field arrays, controlled inputs

This is the biggest React ticket in the project. The form has: name, photo, notes, servings, macros, *a variable-length ingredient list* (ingredient + quantity + unit rows, addable/removable), and *a variable-length prep-item list* (label + lead-time-hours rows).

h3. Subtasks
* Model the whole form in one useReducer - actions like SET_FIELD, ADD_INGREDIENT, UPDATE_INGREDIENT, REMOVE_INGREDIENT, ADD_PREP, ...
* Ingredient rows use an autocomplete against GET /ingredients
* Every dynamic row gets a stable client-generated id as its key (crypto.randomUUID()), *not* the array index
* Client validation before submit; map 422 field errors onto rows
* Reuse the same component for create and edit (/recipes/new and /recipes/:id/edit)

h3. Acceptance criteria
* Add three ingredient rows, fill them, delete the *middle* one - the other two keep their values. (With index keys they will not. Try it wrong first, then fix it - this is the reconciliation lesson.)
* No state mutation anywhere in the reducer - every case returns a new object/array
* Submitting twice fast creates one recipe
* Edit mode pre-populates every field including all dynamic rows
* A server 422 on ingredients[1].quantity highlights that specific row

h3. Definition of done
* All acceptance criteria personally verified in the browser
* {{npm run build}} - zero TS errors
* {{npm run lint}} - zero warnings (incl. react-hooks/exhaustive-deps; if disabled, comment why on that line)
* Loading / error / empty / success all render, if the ticket touches data
* Every effect that subscribes, unsubscribes
* Every fetch is abortable
* No index keys on dynamic lists
* A note in LEARNING.md - the concept, the Angular equivalent, and the one thing that surprised you",,PHASE 4 - Recipes,frontend forms hooks recipes
MP-43 - Photo upload + accessible fields,Story,"*Concepts:* useRef on a file input, object URLs, useId

h3. Subtasks
* Hidden file input triggered by a styled button via useRef
* Local preview with URL.createObjectURL, revoked on cleanup
* useId to wire every <label htmlFor> to its input

h3. Acceptance criteria
* Preview appears before upload completes
* No object-URL leak (revoke in the effect cleanup)
* Every input is reachable by clicking its label
* Upload failure leaves the form usable

h3. Definition of done
* All acceptance criteria personally verified in the browser
* {{npm run build}} - zero TS errors
* {{npm run lint}} - zero warnings (incl. react-hooks/exhaustive-deps; if disabled, comment why on that line)
* Loading / error / empty / success all render, if the ticket touches data
* Every effect that subscribes, unsubscribes
* Every fetch is abortable
* No index keys on dynamic lists
* A note in LEARNING.md - the concept, the Angular equivalent, and the one thing that surprised you",,PHASE 4 - Recipes,frontend forms a11y recipes
MP-44 - Delete with confirmation + optimistic-free flow,Story,"h3. Subtasks
* Confirm modal; on success remove from list without a full refetch

h3. Acceptance criteria
* Deleting the last item on a page shows the empty state
* A failed delete restores the row and shows an error

h3. Definition of done
* All acceptance criteria personally verified in the browser
* {{npm run build}} - zero TS errors
* {{npm run lint}} - zero warnings (incl. react-hooks/exhaustive-deps; if disabled, comment why on that line)
* Loading / error / empty / success all render, if the ticket touches data
* Every effect that subscribes, unsubscribes
* Every fetch is abortable
* No index keys on dynamic lists
* A note in LEARNING.md - the concept, the Angular equivalent, and the one thing that surprised you",,PHASE 4 - Recipes,frontend recipes ux
MP-50 - useDebounce + text search,Story,"*Concepts:* custom hook, timers with cleanup

h3. Subtasks
* {{hooks/useDebounce.ts}}
* Search box on the recipes page hitting ?q=

h3. Acceptance criteria
* Typing ""chicken"" fires *one* request, not eight
* Clearing the box resets to the unfiltered list
* The debounce timer is cleared on unmount

h3. Definition of done
* All acceptance criteria personally verified in the browser
* {{npm run build}} - zero TS errors
* {{npm run lint}} - zero warnings (incl. react-hooks/exhaustive-deps; if disabled, comment why on that line)
* Loading / error / empty / success all render, if the ticket touches data
* Every effect that subscribes, unsubscribes
* Every fetch is abortable
* No index keys on dynamic lists
* A note in LEARNING.md - the concept, the Angular equivalent, and the one thing that surprised you",,PHASE 5 - Find recipes,frontend hooks performance
MP-51 - Pantry filter with useTransition / useDeferredValue,Story,"*Concepts:* concurrent rendering, urgent vs non-urgent updates

h3. Subtasks
* Multi-select of available ingredients (the ""select available ingredients"" filter)
* Results ranked: cookable now -> missing 1 -> missing 2
* Wrap the results update in startTransition, or derive with useDeferredValue
* Show a subtle pending indicator from isPending

h3. Acceptance criteria
* With 200+ recipes rendered, toggling checkboxes stays instantly responsive
* The checkbox visibly updates *before* the list does
* You can articulate the difference between useTransition and useDeferredValue and why you chose one

h3. Definition of done
* All acceptance criteria personally verified in the browser
* {{npm run build}} - zero TS errors
* {{npm run lint}} - zero warnings (incl. react-hooks/exhaustive-deps; if disabled, comment why on that line)
* Loading / error / empty / success all render, if the ticket touches data
* Every effect that subscribes, unsubscribes
* Every fetch is abortable
* No index keys on dynamic lists
* A note in LEARNING.md - the concept, the Angular equivalent, and the one thing that surprised you",,PHASE 5 - Find recipes,frontend performance concurrent
MP-52 - Filters in the URL,Story,"*Concepts:* useSearchParams, URL as state

h3. Subtasks
* Search text and selected ingredients live in the query string

h3. Acceptance criteria
* A filtered URL is copy-pasteable into a new tab and reproduces the exact view
* Back undoes the last filter change
* Refresh preserves filters
* No duplicate fetch on mount from syncing state to URL and back

h3. Definition of done
* All acceptance criteria personally verified in the browser
* {{npm run build}} - zero TS errors
* {{npm run lint}} - zero warnings (incl. react-hooks/exhaustive-deps; if disabled, comment why on that line)
* Loading / error / empty / success all render, if the ticket touches data
* Every effect that subscribes, unsubscribes
* Every fetch is abortable
* No index keys on dynamic lists
* A note in LEARNING.md - the concept, the Angular equivalent, and the one thing that surprised you",,PHASE 5 - Find recipes,frontend routing state
MP-53 - Pagination or infinite scroll,Story,"h3. Subtasks
* Pick one and implement it against the paginated endpoint

h3. Acceptance criteria
* Page state is in the URL
* Changing filters resets to page 1
* In-flight page requests are aborted on filter change

h3. Definition of done
* All acceptance criteria personally verified in the browser
* {{npm run build}} - zero TS errors
* {{npm run lint}} - zero warnings (incl. react-hooks/exhaustive-deps; if disabled, comment why on that line)
* Loading / error / empty / success all render, if the ticket touches data
* Every effect that subscribes, unsubscribes
* Every fetch is abortable
* No index keys on dynamic lists
* A note in LEARNING.md - the concept, the Angular equivalent, and the one thing that surprised you",,PHASE 5 - Find recipes,frontend data-fetching
MP-60 - Weekly planner grid,Story,"*Concepts:* derived data, key on a 2-D structure

h3. Subtasks
* 7 days x 4 meal types grid; week navigation (prev/this/next)
* Each cell lists planned recipes with a remove button

h3. Acceptance criteria
* One {{GET /meal-plan?from=&to=}} per week change, not per cell
* Empty cells show an ""Add"" affordance
* Today's column is visually marked

h3. Definition of done
* All acceptance criteria personally verified in the browser
* {{npm run build}} - zero TS errors
* {{npm run lint}} - zero warnings (incl. react-hooks/exhaustive-deps; if disabled, comment why on that line)
* Loading / error / empty / success all render, if the ticket touches data
* Every effect that subscribes, unsubscribes
* Every fetch is abortable
* No index keys on dynamic lists
* A note in LEARNING.md - the concept, the Angular equivalent, and the one thing that surprised you",,PHASE 6 - Meal plan,frontend planner components
MP-61 - Add-to-plan flow,Story,"h3. Subtasks
* Click a cell -> modal -> pick date/meal type (pre-filled) -> search recipe -> servings -> save
* Also reachable from a recipe detail page (""Add to plan"")

h3. Acceptance criteria
* The grid updates without a full page reload
* Adding the same recipe twice to one slot is prevented with a clear message
* The modal closes only on success

h3. Definition of done
* All acceptance criteria personally verified in the browser
* {{npm run build}} - zero TS errors
* {{npm run lint}} - zero warnings (incl. react-hooks/exhaustive-deps; if disabled, comment why on that line)
* Loading / error / empty / success all render, if the ticket touches data
* Every effect that subscribes, unsubscribes
* Every fetch is abortable
* No index keys on dynamic lists
* A note in LEARNING.md - the concept, the Angular equivalent, and the one thing that surprised you",,PHASE 6 - Meal plan,frontend planner forms
MP-62 - Daily macro totals,Story,"*Concepts:* useMemo on real derived data, and knowing when it is pointless

h3. Subtasks
* Total calories + protein per day, shown under each day column
* Compute client-side from loaded plan data

h3. Acceptance criteria
* Totals update immediately on add/remove
* The useMemo dependency array is exactly the plan slice it reads
* *And* you have measured whether the memo actually helps here - if it does not, remove it and write down why (this is a legitimate outcome of the ticket)

h3. Definition of done
* All acceptance criteria personally verified in the browser
* {{npm run build}} - zero TS errors
* {{npm run lint}} - zero warnings (incl. react-hooks/exhaustive-deps; if disabled, comment why on that line)
* Loading / error / empty / success all render, if the ticket touches data
* Every effect that subscribes, unsubscribes
* Every fetch is abortable
* No index keys on dynamic lists
* A note in LEARNING.md - the concept, the Angular equivalent, and the one thing that surprised you",,PHASE 6 - Meal plan,frontend planner performance
MP-63 - Week navigation with keyboard shortcuts,Story,"*Concepts:* useEffect event listeners, useRef for non-render values

h3. Subtasks
* Left/right arrows change week; {{t}} jumps to today

h3. Acceptance criteria
* Listener is removed on unmount (add/remove logged to prove it)
* Shortcuts do not fire while typing in an input

h3. Definition of done
* All acceptance criteria personally verified in the browser
* {{npm run build}} - zero TS errors
* {{npm run lint}} - zero warnings (incl. react-hooks/exhaustive-deps; if disabled, comment why on that line)
* Loading / error / empty / success all render, if the ticket touches data
* Every effect that subscribes, unsubscribes
* Every fetch is abortable
* No index keys on dynamic lists
* A note in LEARNING.md - the concept, the Angular equivalent, and the one thing that surprised you",,PHASE 6 - Meal plan,frontend planner a11y hooks
MP-64 - useLocalStorage for view preferences,Story,"*Concepts:* custom hook, lazy useState init

h3. Subtasks
* Persist last-viewed week and grid/list toggle

h3. Acceptance criteria
* Preference survives refresh
* A corrupt localStorage value does not crash the app (the try/catch in the lazy initialiser)

h3. Definition of done
* All acceptance criteria personally verified in the browser
* {{npm run build}} - zero TS errors
* {{npm run lint}} - zero warnings (incl. react-hooks/exhaustive-deps; if disabled, comment why on that line)
* Loading / error / empty / success all render, if the ticket touches data
* Every effect that subscribes, unsubscribes
* Every fetch is abortable
* No index keys on dynamic lists
* A note in LEARNING.md - the concept, the Angular equivalent, and the one thing that surprised you",,PHASE 6 - Meal plan,frontend hooks planner
MP-70 - Prep-timer todo list,Story,"*Concepts:* consuming derived server data, date handling, optimistic toggle

h3. Subtasks
* *First:* write the bucketing rule from Section 4 of the roadmap down as prose, agree it, then implement
* /todos page grouped by day -> meal slot
* Checkbox toggle with optimistic UI, rolled back on failure

h3. Acceptance criteria
* Planning Tuesday lunch beans (12h soak) produces a todo in the agreed slot - assert the exact expected timestamp in the ticket
* Unchecking and rechecking survives a refresh
* Removing the plan entry removes the undone todo
* A failed toggle visibly reverts

h3. Definition of done
* All acceptance criteria personally verified in the browser
* {{npm run build}} - zero TS errors
* {{npm run lint}} - zero warnings (incl. react-hooks/exhaustive-deps; if disabled, comment why on that line)
* Loading / error / empty / success all render, if the ticket touches data
* Every effect that subscribes, unsubscribes
* Every fetch is abortable
* No index keys on dynamic lists
* A note in LEARNING.md - the concept, the Angular equivalent, and the one thing that surprised you",,PHASE 7 - Todos & shopping list,frontend todos dates
MP-71 - Todo timer / countdown,Story,"*Concepts:* setInterval in an effect, cleanup, stale closures

h3. Subtasks
* Relative ""due in 4h 20m"" that ticks

h3. Acceptance criteria
* One interval per mounted component, cleared on unmount
* The displayed value uses fresh state, not a stale closure (the classic setInterval + useState bug - hit it, then fix it with the updater form or a ref)

h3. Definition of done
* All acceptance criteria personally verified in the browser
* {{npm run build}} - zero TS errors
* {{npm run lint}} - zero warnings (incl. react-hooks/exhaustive-deps; if disabled, comment why on that line)
* Loading / error / empty / success all render, if the ticket touches data
* Every effect that subscribes, unsubscribes
* Every fetch is abortable
* No index keys on dynamic lists
* A note in LEARNING.md - the concept, the Angular equivalent, and the one thing that surprised you",,PHASE 7 - Todos & shopping list,frontend todos hooks
MP-72 - Weekly shopping list,Story,"h3. Subtasks
* /shopping?from=&to= - grouped by ingredient category, quantities summed, unit-aware
* Check-off state persisted locally

h3. Acceptance criteria
* Two recipes using 200g + 300g chickpeas show one 500g line
* Mismatched units (g vs cups) are shown separately rather than wrongly summed
* The list recalculates when the plan changes

h3. Definition of done
* All acceptance criteria personally verified in the browser
* {{npm run build}} - zero TS errors
* {{npm run lint}} - zero warnings (incl. react-hooks/exhaustive-deps; if disabled, comment why on that line)
* Loading / error / empty / success all render, if the ticket touches data
* Every effect that subscribes, unsubscribes
* Every fetch is abortable
* No index keys on dynamic lists
* A note in LEARNING.md - the concept, the Angular equivalent, and the one thing that surprised you",,PHASE 7 - Todos & shopping list,frontend shopping
MP-73 - Export / print view,Story,"h3. Subtasks
* A print stylesheet or copy-to-clipboard

h3. Acceptance criteria
* Printed output has no nav chrome

h3. Definition of done
* All acceptance criteria personally verified in the browser
* {{npm run build}} - zero TS errors
* {{npm run lint}} - zero warnings (incl. react-hooks/exhaustive-deps; if disabled, comment why on that line)
* Loading / error / empty / success all render, if the ticket touches data
* Every effect that subscribes, unsubscribes
* Every fetch is abortable
* No index keys on dynamic lists
* A note in LEARNING.md - the concept, the Angular equivalent, and the one thing that surprised you",,PHASE 7 - Todos & shopping list,frontend shopping ux
MP-80 - Today's panel,Story,"h3. Subtasks
* Today's meals by slot, today's todos, today's macro totals

h3. Acceptance criteria
* One round trip for the whole dashboard (or a documented reason why more)
* Each panel has its own loading/empty state so a slow panel does not block the others

h3. Definition of done
* All acceptance criteria personally verified in the browser
* {{npm run build}} - zero TS errors
* {{npm run lint}} - zero warnings (incl. react-hooks/exhaustive-deps; if disabled, comment why on that line)
* Loading / error / empty / success all render, if the ticket touches data
* Every effect that subscribes, unsubscribes
* Every fetch is abortable
* No index keys on dynamic lists
* A note in LEARNING.md - the concept, the Angular equivalent, and the one thing that surprised you",,PHASE 8 - Dashboard,frontend dashboard
MP-81 - Week-at-a-glance,Story,"h3. Subtasks
* Compact 7-day strip linking into the planner

h3. Acceptance criteria
* Links carry the week into /planner via search params

h3. Definition of done
* All acceptance criteria personally verified in the browser
* {{npm run build}} - zero TS errors
* {{npm run lint}} - zero warnings (incl. react-hooks/exhaustive-deps; if disabled, comment why on that line)
* Loading / error / empty / success all render, if the ticket touches data
* Every effect that subscribes, unsubscribes
* Every fetch is abortable
* No index keys on dynamic lists
* A note in LEARNING.md - the concept, the Angular equivalent, and the one thing that surprised you",,PHASE 8 - Dashboard,frontend dashboard
MP-82 - Suggest more like this,Story,"h3. Subtasks
* Links from today's ingredients to the pantry-filtered recipe search

h3. Acceptance criteria
* The link lands on /recipes with the filters pre-applied via URL (reusing MP-52 - no new state mechanism)

h3. Definition of done
* All acceptance criteria personally verified in the browser
* {{npm run build}} - zero TS errors
* {{npm run lint}} - zero warnings (incl. react-hooks/exhaustive-deps; if disabled, comment why on that line)
* Loading / error / empty / success all render, if the ticket touches data
* Every effect that subscribes, unsubscribes
* Every fetch is abortable
* No index keys on dynamic lists
* A note in LEARNING.md - the concept, the Angular equivalent, and the one thing that surprised you",,PHASE 8 - Dashboard,frontend dashboard
MP-90 - Measure first,Story,"Do this phase *after* the app works. That is the point: you profile a real app, not a hypothetical one.

h3. Subtasks
* React DevTools Profiler: record interactions on the recipe list, planner grid, and search
* Enable ""Highlight updates when components render""
* Write a short findings note: which components re-render that should not, with commit durations

h3. Acceptance criteria
* You have a written before-list of at least three concrete over-rendering problems, with numbers. No fixes yet.

h3. Definition of done
* All acceptance criteria personally verified in the browser
* {{npm run build}} - zero TS errors
* {{npm run lint}} - zero warnings (incl. react-hooks/exhaustive-deps; if disabled, comment why on that line)
* Loading / error / empty / success all render, if the ticket touches data
* Every effect that subscribes, unsubscribes
* Every fetch is abortable
* No index keys on dynamic lists
* A note in LEARNING.md - the concept, the Angular equivalent, and the one thing that surprised you",,PHASE 9 - Performance pass,frontend performance profiling
MP-91 - React.memo + useCallback + stable props,Story,"Do this phase *after* the app works. That is the point: you profile a real app, not a hypothetical one.

h3. Subtasks
* Fix the findings from MP-90 - memo on RecipeCard/PlannerCell, useCallback on handlers passed to them, useMemo on object/array props

h3. Acceptance criteria
* Re-profile and show each finding measurably improved
* Every memo you added is justified by a number in the MP-90 note
* You removed at least one memo that did not help (there will be one)

h3. Definition of done
* All acceptance criteria personally verified in the browser
* {{npm run build}} - zero TS errors
* {{npm run lint}} - zero warnings (incl. react-hooks/exhaustive-deps; if disabled, comment why on that line)
* Loading / error / empty / success all render, if the ticket touches data
* Every effect that subscribes, unsubscribes
* Every fetch is abortable
* No index keys on dynamic lists
* A note in LEARNING.md - the concept, the Angular equivalent, and the one thing that surprised you",,PHASE 9 - Performance pass,frontend performance
MP-92 - Context splitting & state colocation,Story,"*Concepts:* the re-render cost of context, moving state down

Do this phase *after* the app works. That is the point: you profile a real app, not a hypothetical one.

h3. Subtasks
* Split any context where consumers only need part of it (e.g. auth user vs auth actions)
* Find state living too high and colocate it downward

h3. Acceptance criteria
* A component consuming only auth *actions* does not re-render when user changes
* At least one piece of state moved down and the parent's render count dropped

h3. Definition of done
* All acceptance criteria personally verified in the browser
* {{npm run build}} - zero TS errors
* {{npm run lint}} - zero warnings (incl. react-hooks/exhaustive-deps; if disabled, comment why on that line)
* Loading / error / empty / success all render, if the ticket touches data
* Every effect that subscribes, unsubscribes
* Every fetch is abortable
* No index keys on dynamic lists
* A note in LEARNING.md - the concept, the Angular equivalent, and the one thing that surprised you",,PHASE 9 - Performance pass,frontend performance state
MP-93 - Virtualization,Story,"Do this phase *after* the app works. That is the point: you profile a real app, not a hypothetical one.

h3. Subtasks
* @tanstack/react-virtual on the recipe list (seed 500+ recipes to make it real)

h3. Acceptance criteria
* DOM node count stays roughly constant while scrolling 500 rows
* Scroll stays at 60fps
* Row heights do not jump

h3. Definition of done
* All acceptance criteria personally verified in the browser
* {{npm run build}} - zero TS errors
* {{npm run lint}} - zero warnings (incl. react-hooks/exhaustive-deps; if disabled, comment why on that line)
* Loading / error / empty / success all render, if the ticket touches data
* Every effect that subscribes, unsubscribes
* Every fetch is abortable
* No index keys on dynamic lists
* A note in LEARNING.md - the concept, the Angular equivalent, and the one thing that surprised you",,PHASE 9 - Performance pass,frontend performance
MP-94 - Error boundaries,Story,"Do this phase *after* the app works. That is the point: you profile a real app, not a hypothetical one.

h3. Subtasks
* A boundary around the router's lazy routes; errorElement on route branches
* Chunk-load failure offers a reload

h3. Acceptance criteria
* Throwing inside a page shows the boundary, not a white screen
* The rest of the shell (nav) still works
* A simulated failed chunk load offers reload

h3. Definition of done
* All acceptance criteria personally verified in the browser
* {{npm run build}} - zero TS errors
* {{npm run lint}} - zero warnings (incl. react-hooks/exhaustive-deps; if disabled, comment why on that line)
* Loading / error / empty / success all render, if the ticket touches data
* Every effect that subscribes, unsubscribes
* Every fetch is abortable
* No index keys on dynamic lists
* A note in LEARNING.md - the concept, the Angular equivalent, and the one thing that surprised you",,PHASE 9 - Performance pass,frontend error-handling
MP-95 - Prefetch on hover,Story,"Do this phase *after* the app works. That is the point: you profile a real app, not a hypothetical one.

h3. Subtasks
* onMouseEnter triggers the route's dynamic import()

h3. Acceptance criteria
* Hovering a nav link fetches the chunk before the click
* Navigation after hovering shows no skeleton

h3. Definition of done
* All acceptance criteria personally verified in the browser
* {{npm run build}} - zero TS errors
* {{npm run lint}} - zero warnings (incl. react-hooks/exhaustive-deps; if disabled, comment why on that line)
* Loading / error / empty / success all render, if the ticket touches data
* Every effect that subscribes, unsubscribes
* Every fetch is abortable
* No index keys on dynamic lists
* A note in LEARNING.md - the concept, the Angular equivalent, and the one thing that surprised you",,PHASE 9 - Performance pass,frontend performance routing
MP-100 - React 19 form APIs,Story,"h3. Subtasks
* Rebuild the login or recipe form with useActionState + useFormStatus
* Use useOptimistic for the todo toggle
* Compare with your hand-rolled version

h3. Definition of done
* All acceptance criteria personally verified in the browser
* {{npm run build}} - zero TS errors
* {{npm run lint}} - zero warnings (incl. react-hooks/exhaustive-deps; if disabled, comment why on that line)
* Loading / error / empty / success all render, if the ticket touches data
* Every effect that subscribes, unsubscribes
* Every fetch is abortable
* No index keys on dynamic lists
* A note in LEARNING.md - the concept, the Angular equivalent, and the one thing that surprised you",,PHASE 10 - Stretch,frontend stretch react19 forms
MP-101 - Replace the fetch hooks with TanStack Query,Story,"h3. Subtasks
* Replace MP-31/32's hooks with TanStack Query
* Write up what it gave you (cache, dedupe, refetch, stale-while-revalidate) and what you lost

h3. Acceptance criteria
* The retrospective write-up is the actual deliverable

h3. Definition of done
* All acceptance criteria personally verified in the browser
* {{npm run build}} - zero TS errors
* {{npm run lint}} - zero warnings (incl. react-hooks/exhaustive-deps; if disabled, comment why on that line)
* Loading / error / empty / success all render, if the ticket touches data
* Every effect that subscribes, unsubscribes
* Every fetch is abortable
* No index keys on dynamic lists
* A note in LEARNING.md - the concept, the Angular equivalent, and the one thing that surprised you",,PHASE 10 - Stretch,frontend stretch data-fetching
MP-102 - useSyncExternalStore,Story,"h3. Subtasks
* useSyncExternalStore for online/offline status
* Cross-tab sync of the shopping list

h3. Definition of done
* All acceptance criteria personally verified in the browser
* {{npm run build}} - zero TS errors
* {{npm run lint}} - zero warnings (incl. react-hooks/exhaustive-deps; if disabled, comment why on that line)
* Loading / error / empty / success all render, if the ticket touches data
* Every effect that subscribes, unsubscribes
* Every fetch is abortable
* No index keys on dynamic lists
* A note in LEARNING.md - the concept, the Angular equivalent, and the one thing that surprised you",,PHASE 10 - Stretch,frontend stretch hooks
MP-103 - Tests,Story,"h3. Subtasks
* Vitest + Testing Library on the reducer (MP-42)
* The prep-timer date math (MP-70)
* One integration test of the login flow with MSW

h3. Definition of done
* All acceptance criteria personally verified in the browser
* {{npm run build}} - zero TS errors
* {{npm run lint}} - zero warnings (incl. react-hooks/exhaustive-deps; if disabled, comment why on that line)
* Loading / error / empty / success all render, if the ticket touches data
* Every effect that subscribes, unsubscribes
* Every fetch is abortable
* No index keys on dynamic lists
* A note in LEARNING.md - the concept, the Angular equivalent, and the one thing that surprised you",,PHASE 10 - Stretch,frontend stretch testing
MP-104 - Deploy,Story,"h3. Subtasks
* Client to Vercel/Netlify
* Server + Postgres to Railway/Fly/Neon

h3. Definition of done
* All acceptance criteria personally verified in the browser
* {{npm run build}} - zero TS errors
* {{npm run lint}} - zero warnings (incl. react-hooks/exhaustive-deps; if disabled, comment why on that line)
* Loading / error / empty / success all render, if the ticket touches data
* Every effect that subscribes, unsubscribes
* Every fetch is abortable
* No index keys on dynamic lists
* A note in LEARNING.md - the concept, the Angular equivalent, and the one thing that surprised you",,PHASE 10 - Stretch,stretch devops
